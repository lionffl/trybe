const sum = (a,b) => console.log(`${a} + ${b} is equal to: ${a + b}`);
const sub = (a,b) => console.log(`${a} - ${b} is equal to: ${a - b}`);
const mul = (a,b) => console.log(`${a} * ${b} is equal to: ${a * b}`);
const div = (a,b) => console.log(`${a} / ${b} is equal to: ${a / b}`);

module.exports = {
  sum,
  sub,
  mul,
  div
}