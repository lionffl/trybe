// Make a constant called `myName` and set your name as its value

const myName = "Felipe";

// Make a constant called `birthCity` and set your birth city as its value

const birthCity = "Fortaleza";

// Make a variable called `birthYear` and set your birth year as its value

let birthYear = 1988;

// Print the constants and variables you just defined

console.log(myName + " " + birthCity + " " + birthYear);

// Change the value of birthYear variable to 2030 and print

birthYear = 2030;

console.log(birthYear);

// Try to change the value of birthCity constant and explain the results

birthCity = "Natal";

// R: We can't change a constant value.