// Use the code below and print the variable types.

let patientId = 50;
let isEnrolled = true;
const patientInfo = {
  firstName: 'Ana',
  lastName: 'Santos',
};
const patientEmail = 'ana@email.com';

console.log(`${typeof patientId} ${typeof isEnrolled} ${typeof patientInfo} ${typeof patientEmail}`);

// Set a constant named `base` and a variable named `height` with 5 and 8 values

const base = 5;
let height = 8;

// Set a constant named `area` with the value of base * height

const area = base * height;

console.log(area);

// Set a constant named `perimeter` with the value of base + height

const perimeter = base + height;

console.log(perimeter);
